{
  "name": "blackcheck-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Blackcheck platform with Bamcheat-compatible frontend and API",
  "dependencies": {
    "mysql2": "^3.14.3"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check comment-utils.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
